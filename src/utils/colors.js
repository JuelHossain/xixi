const colors = require("@juel/hooks/colors");
const lightColors = {
  main: colors.purple,
  sec: colors.purple,
  neu: colors.gray,
};
const darkColors = {
  main: colors.green,
  sec: colors.rose,
  neu: colors.gray,
};

module.exports = { lightColors, darkColors };
